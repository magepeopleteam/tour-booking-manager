<?php	if ( ! defined( 'ABSPATH' ) ) {		die;	}	$tour_id     = $tour_id ?? get_the_id();	$class_title = $class_title ?? '';	$title       = get_the_title( $tour_id );?>	<div class="ttbm_list_title <?php echo esc_attr( $class_title ); ?>" data-placeholder>		<h5>			<a href="<?php echo get_the_permalink( $tour_id ); ?>">				<?php echo esc_html( $title ); ?>			</a>		</h5>	</div><?php do_action( 'ttbm_title_after', $tour_id ); ?>